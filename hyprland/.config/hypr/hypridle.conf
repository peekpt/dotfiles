# Set the idle time in seconds (e.g., 300 seconds = 5 minutes)
idle_timeout = 300

# The command to execute when the idle timeout is reached
lock_command = "hyprlock"

# Optional: Use a custom idle time detection method (default is 10 seconds)
idle_check_interval = 10

# Optional: Set a custom idle time to trigger lock for specific apps
# app_idle_timeout = 600   # This would trigger after 10 minutes of idle time for specific apps
